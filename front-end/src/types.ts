export interface PortfolioItem {
    id: string;
    title: string;
    description: string;
    link: string;
    imagesID: string;
}

export interface ImageItem {
    id: string;
    URLs: [string];
}
  
  