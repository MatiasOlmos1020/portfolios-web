export interface PortfolioItem {
    id: string;
    title: string;
    description: string;
    link: string;
    imageurl: string;
}

export interface ImageItem {
    id: string;
    url: string;
}
  
  